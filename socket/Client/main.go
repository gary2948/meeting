package main

import (
	//"code.google.com/p/go.net/websocket"
	"fmt"
	//"net/http"
)

func main() {
	fmt.Printf("Welcome lcsoft server!")

	//http,js璋冪敤
	//http.Handle("/", websocket.Handler(WsServer))

	//	h := new(websocket.Server)
	//	h.Handler = websocket.Handler(WsServer)
	//
	//	err := http.ListenAndServe(":8000", h)
	//	if err != nil {
	//		fmt.Printf("ListenAndServe: " + err.Error())
	//	}

}
